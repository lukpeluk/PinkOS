#include <programs.h>
#include <graphicsLib.h>
#include <stdin.h>
#include <stdpink.h>
#include <pictures.h>


void pietra_main(unsigned char *args) {
    return;

    // Point position_image = {0};
    // Point position_text = {0};

    // int screen_width = getScreenWidth();
    // int screen_height = getScreenHeight();

    // int scale = 10;

    // // Center the image
    // position_image.x = (screen_width - PIETRA_WIDTH * scale) / 2;
    // position_image.y = (screen_height - PIETRA_HEIGHT * scale) / 2;

    // position_text.x = (uint64_t)(position_image.x + 100);
    // position_text.y = (uint64_t)(position_image.y + ((PIETRA_WIDTH * scale)) + 10); 
    // // Draw the Mona Lisa image
    // drawBitmap(pietra, PIETRA_WIDTH, PIETRA_HEIGHT, position_image, scale);
    // drawString("*YMCA plays in the background*", 0x00df8090, 0x00000000, position_text);



    // // Wait for key
    // unsigned char c = get_char_from_stdin();
    // while(1){
    //     if (c != 0) {
    //         break;
    //     }
    //     c = get_char_from_stdin();
    // }
}